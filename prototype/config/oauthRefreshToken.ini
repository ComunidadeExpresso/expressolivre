service = PostgreSQL
PostgreSQL.concept = rest_refresh_token

[PostgreSQL.mapping]
id = refresh_token
refresh_token = refresh_token
client_id = client_id
user_id = user_id
expires = expires
scope = scope
refresh_token = refresh_token