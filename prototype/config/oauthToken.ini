service = PostgreSQL
PostgreSQL.concept = rest_access_token

[PostgreSQL.mapping]
id = oauth_token
oauth_token = oauth_token
client_id = client_id
user_id = user_id
expires = expires
scope = scope
refresh_token = refresh_token
